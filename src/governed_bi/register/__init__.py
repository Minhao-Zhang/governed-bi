"""Declared tables. stdlib only, plus ``governed_bi.ports``.

Not a pipeline stage. Registers declare values; predicates live beside the
types they test. Consumers may iterate these tables, not filter them with a
default. Closure checks live in ``tests/conformance/``.
"""

from __future__ import annotations

__all__: list[str] = []
