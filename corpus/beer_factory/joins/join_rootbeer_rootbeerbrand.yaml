asset_type: join
id: join_rootbeer_rootbeerbrand

# ── Facts ──
left_table: tbl_beer_factory_rootbeer
right_table: tbl_beer_factory_rootbeerbrand
on: "rootbeer.BrandID = rootbeerbrand.BrandID"

# ── Inference ──
cardinality: many_to_one
cost: 1.0
confidence: 0.95

# ── Audit ──
audit:
  evidence: "declared foreign key; each unit belongs to one brand"
  provenance: { source: curator, status: draft }
