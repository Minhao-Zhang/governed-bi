asset_type: join
id: join_transaction_rootbeer

# ── Facts ──
left_table: tbl_beer_factory_transaction
right_table: tbl_beer_factory_rootbeer
on: "transaction.RootBeerID = rootbeer.RootBeerID"

# ── Inference ──
cardinality: many_to_one
cost: 1.0
confidence: 0.95

# ── Audit ──
audit:
  evidence: "declared foreign key; each sale is of one root beer unit"
  provenance: { source: curator, status: draft }
