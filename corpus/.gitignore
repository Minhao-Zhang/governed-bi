# Derived, rebuildable projections (D9): search index, embeddings, compiled
# graph. Never authored directly, so never committed.
#
# This used to read "Git is the single source of truth", which was false: no corpus
# asset is tracked anywhere in this repository. See ../corpus/README.md.
_generated/
*/_generated/
